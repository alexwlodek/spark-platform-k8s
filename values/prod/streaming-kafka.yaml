# PROD consumes a managed Kafka endpoint instead of deploying in-cluster Kafka.
# Keep this file minimal because the broker endpoint is supplied through
# `values/prod/streaming-pipeline.yaml` once the managed Kafka service is ready.
