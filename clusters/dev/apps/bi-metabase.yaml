apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: bi-metabase
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "36"
spec:
  project: platform
  sources:
    - repoURL: https://github.com/alexwlodek/gitops-data-platform.git
      targetRevision: main
      path: charts/bi-metabase
      helm:
        releaseName: bi-metabase
        valueFiles:
          - $values/values/common/bi-metabase.yaml
          - $values/values/dev/bi-metabase.yaml
    - repoURL: https://github.com/alexwlodek/gitops-data-platform.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: apps
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
