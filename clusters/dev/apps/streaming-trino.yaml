apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: streaming-trino
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "34"
spec:
  project: platform
  sources:
    - repoURL: https://github.com/alexwlodek/spark-platform-k8s.git
      targetRevision: main
      path: charts/streaming-trino
      helm:
        releaseName: streaming-trino
        valueFiles:
          - $values/values/common/streaming-trino.yaml
          - $values/values/dev/streaming-trino.yaml
    - repoURL: https://github.com/alexwlodek/spark-platform-k8s.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: apps
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
