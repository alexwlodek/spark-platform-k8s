apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: streaming-metabase
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "36"
spec:
  project: platform
  sources:
    - repoURL: https://github.com/alexwlodek/spark-platform-k8s.git
      targetRevision: main
      path: charts/streaming-metabase
      helm:
        releaseName: streaming-metabase
        valueFiles:
          - $values/values/common/streaming-metabase.yaml
          - $values/values/dev/streaming-metabase.yaml
    - repoURL: https://github.com/alexwlodek/spark-platform-k8s.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: apps
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
