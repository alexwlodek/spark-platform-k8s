apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: streaming-nessie
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "33"
spec:
  project: platform
  sources:
    - repoURL: https://github.com/alexwlodek/spark-platform-k8s.git
      targetRevision: main
      path: charts/streaming-nessie
      helm:
        releaseName: streaming-nessie
        valueFiles:
          - $values/values/common/streaming-nessie.yaml
          - $values/values/dev/streaming-nessie.yaml
    - repoURL: https://github.com/alexwlodek/spark-platform-k8s.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: apps
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
