apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: storage-nessie-db-credentials
  namespace: apps
  annotations:
    argocd.argoproj.io/sync-wave: "23"
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: aws-secretsmanager
  target:
    name: platform-nessie-db-creds
    creationPolicy: Owner
    deletionPolicy: Retain
  data:
    - secretKey: database
      remoteRef:
        key: /spark-platform/dev/storage-nessie-db
        property: database
    - secretKey: username
      remoteRef:
        key: /spark-platform/dev/storage-nessie-db
        property: username
    - secretKey: password
      remoteRef:
        key: /spark-platform/dev/storage-nessie-db
        property: password
    - secretKey: postgres-password
      remoteRef:
        key: /spark-platform/dev/storage-nessie-db
        property: postgres-password
