apiVersion: v1
kind: Service
metadata:
  name: {{ include "streaming-trino.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "streaming-trino.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
  selector:
    app.kubernetes.io/instance: {{ .Release.Name | quote }}
    app.kubernetes.io/name: {{ include "streaming-trino.name" . | quote }}
