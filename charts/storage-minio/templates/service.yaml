apiVersion: v1
kind: Service
metadata:
  name: {{ include "storage-minio.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "storage-minio.labels" . | indent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: api
      port: {{ .Values.service.apiPort }}
      targetPort: api
    - name: console
      port: {{ .Values.service.consolePort }}
      targetPort: console
  selector:
    app.kubernetes.io/instance: {{ .Release.Name | quote }}
    app.kubernetes.io/name: {{ include "storage-minio.name" . | quote }}
